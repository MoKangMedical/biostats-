# 🚀 Biostats终结者 - 腾讯云部署指南

## 🎯 你选择了：腾讯云轻量应用服务器

**优势**：
- 💰 第一年仅需 **¥88**
- ⚡ 配置：2核4GB + 80GB + 1200GB流量
- 🇨🇳 国内访问速度快（延迟<50ms）
- 📱 支持微信登录和支付

---

## ✅ 第1步：注册腾讯云账户（5分钟）

### 1.1 访问腾讯云

**立即访问**：
```
https://cloud.tencent.com
```

在浏览器中打开这个链接。

---

### 1.2 注册账户

你会看到腾讯云首页，**点击右上角"注册"按钮**

**有3种注册方式**：

**方式A：微信扫码注册**（最快！推荐）⭐
```
1. 点击"微信"图标
2. 用微信扫描二维码
3. 确认授权
4. 完成注册！

✅ 优势：1分钟搞定，无需输入信息
```

**方式B：QQ登录**
```
如果你有QQ号，直接用QQ登录即可
```

**方式C：邮箱注册**
```
1. 输入邮箱地址
2. 设置密码
3. 输入验证码
4. 点击"同意协议并提交"
```

**推荐使用方式A：微信扫码，最快！**

---

### 1.3 完善账户信息

注册后，系统会要求补充信息：

```
昵称：____________（随意填写）
手机号：186XXXXXXXX（用于接收验证码）
短信验证码：XXXXXX
```

点击"提交"

---

## ✅ 第2步：实名认证（必须，10分钟）

**⚠️ 重要**：购买服务器前必须完成实名认证！

### 2.1 进入实名认证页面

**方法1**：
```
访问：https://console.cloud.tencent.com/developer
```

**方法2**：
```
登录后 → 点击右上角头像 → 账号信息 → 实名认证
```

---

### 2.2 选择认证类型

**个人认证**（推荐，快速）
```
适用于：个人用户、学习测试
审核时间：立即通过
```

**企业认证**
```
适用于：公司、团队
审核时间：1-3个工作日
```

**选择"个人认证"** ← 点这个

---

### 2.3 完成个人认证

**有3种方式，选择最方便的**：

**方式A：微信认证**（最快！5分钟）⭐⭐⭐
```
1. 点击"微信认证"
2. 微信扫码
3. 输入姓名、身份证号
4. 人脸识别（按提示操作）
5. 完成！立即通过

✅ 推荐这个方式！
```

**方式B：银行卡认证**（1分钟）
```
1. 输入姓名
2. 输入身份证号
3. 输入银行卡号
4. 验证手机号
5. 完成

注意：只是验证，不会扣款
```

**方式C：人工审核**（较慢，1个工作日）
```
1. 上传身份证正反面照片
2. 填写身份信息
3. 等待审核
```

**推荐使用方式A：微信认证，最快！**

---

### 2.4 验证认证成功

认证完成后，页面会显示：
```
✓ 实名认证：已认证
状态：个人认证
姓名：***
身份证号：******************
```

✅ 认证成功！可以购买服务器了！

---

## ✅ 第3步：购买轻量应用服务器（10分钟）

### 3.1 访问购买页面

**直接访问优惠页面**：
```
https://cloud.tencent.com/act/pro/lighthouse
```

或者：
```
腾讯云首页 → 产品 → 轻量应用服务器 → 立即选购
```

---

### 3.2 选择配置

你会看到购买页面，现在开始选择配置：

**【地域】** - 选择服务器位置
```
根据你的位置选择：

华南地区（广州、深圳）
├─ 广州 ← 推荐华南用户
└─ 深圳

华东地区（上海、南京）
└─ 上海 ← 推荐华东用户

华北地区（北京）
└─ 北京 ← 推荐华北用户

西南地区（成都）
└─ 成都 ← 推荐西南用户

🎯 推荐：选择离你最近的地域
例如：在北京就选北京，在上海就选上海
```

**【镜像】** - 选择操作系统
```
点击"应用镜像"标签
找到并选择：

✓ Docker CE（推荐！已预装Docker）

或者选择"系统镜像"标签：
✓ Ubuntu 22.04 LTS

推荐选择：Docker CE（省去安装Docker的步骤）
```

**【套餐】** - 选择配置
```
向下滚动，找到推荐配置：

┌─────────────────────────────────┐
│ 2核4GB 80GB SSD                  │
│ 1200GB月流量                     │
│ 峰值带宽 8Mbps                   │
│                                 │
│ 首年：¥88/年  ← 选这个！         │
│ 续费：¥336/年                    │
└─────────────────────────────────┘

✅ 点击选中这个套餐
```

**【购买时长】**
```
选择：
✓ 1年（推荐，¥88优惠价）

或
○ 1个月（¥28，测试用）
○ 3个月（¥84）
```

**【数量】**
```
保持默认：1台
```

**【服务器名称】**
```
自定义名称：biostats-server

或保持默认也可以
```

---

### 3.3 设置密码（重要！）

**向下滚动，找到"设置密码"**

```
✓ 自定义密码（推荐）

输入密码：________________
确认密码：________________

⚠️ 密码要求：
- 8-30位字符
- 必须包含大小写字母、数字
- 可以包含特殊符号

例如：BioStats2024!

🔴 非常重要：记住这个密码！
```

---

### 3.4 确认订单

**检查配置**：
```
地域：上海/北京/广州（你选的）
镜像：Docker CE 或 Ubuntu 22.04
套餐：2核4GB
时长：1年
价格：¥88
```

**勾选协议**：
```
☑ 我已阅读并同意《腾讯云服务协议》
```

**点击"立即购买"**

---

### 3.5 支付

**选择支付方式**：

```
方式1：微信支付 ⭐
├─ 扫码支付
└─ 最快捷

方式2：支付宝
├─ 跳转支付宝
└─ 也很方便

方式3：QQ钱包
└─ 如果你用QQ

方式4：网银支付
└─ 传统方式
```

**推荐：微信支付（最快）**

**支付金额**：¥88

**完成支付**！

---

### 3.6 等待创建

```
支付成功后：
├─ 自动跳转到控制台
├─ 创建服务器（约1-2分钟）
└─ 创建完成！
```

---

## ✅ 第4步：查看服务器信息（2分钟）

### 4.1 进入控制台

**访问**：
```
https://console.cloud.tencent.com/lighthouse/instance
```

或：
```
登录腾讯云 → 控制台 → 轻量应用服务器
```

---

### 4.2 查看服务器列表

你会看到类似这样：

```
实例列表
┌────────────────────────────────────────────┐
│ biostats-server                            │
│ 运行中 ✓                                   │
│                                            │
│ 公网IP: 43.XXX.XXX.XXX  ← 这是IP地址！     │
│                                            │
│ 地域: 上海                                  │
│ 配置: 2核4GB                               │
│ 系统: Ubuntu 22.04 / Docker CE             │
│                                            │
│ 创建时间: 2分钟前                           │
│ 到期时间: 2027-02-07                       │
└────────────────────────────────────────────┘
```

---

### 4.3 记录重要信息

**📋 立即记录下来**：

```
╔═══════════════════════════════════════════╗
║      腾讯云服务器信息记录                    ║
╠═══════════════════════════════════════════╣
║                                           ║
║ 服务器名称: biostats-server               ║
║                                           ║
║ 公网IP: ___.___.___.___ ← 记这个！         ║
║                                           ║
║ 用户名: root                               ║
║                                           ║
║ 密码: ___________________                 ║
║                                           ║
║ 地域: ___________________                 ║
║                                           ║
║ 到期时间: 2027年2月7日                     ║
║                                           ║
╚═══════════════════════════════════════════╝
```

---

## ✅ 第5步：配置防火墙（重要！3分钟）

### 5.1 进入防火墙设置

```
1. 在服务器列表，点击你的服务器名称
2. 点击"防火墙"标签
3. 点击"添加规则"
```

---

### 5.2 添加必要规则

**已有的规则（确认存在）**：
```
✓ SSH (22) - 远程登录
✓ HTTP (80) - 网站访问
✓ HTTPS (443) - 安全访问
```

**需要添加的规则**：

**规则1：应用端口**
```
点击"添加规则"

应用类型：自定义
协议：TCP
端口：8000
策略：允许
来源：0.0.0.0/0
备注：应用访问

点击"确定"
```

**规则2：数据库端口**（可选）
```
应用类型：自定义
协议：TCP
端口：5432
策略：允许
来源：0.0.0.0/0
备注：PostgreSQL

点击"确定"
```

**完成后，防火墙规则列表应该有**：
```
✓ SSH (22)
✓ HTTP (80)
✓ HTTPS (443)
✓ TCP (8000) - 新添加
✓ TCP (5432) - 新添加（可选）
```

---

## ✅ 第6步：连接服务器（2分钟）

### 方法1：使用腾讯云WebShell（最简单）⭐

**步骤**：
```
1. 在服务器列表，找到你的服务器
2. 点击右侧"登录"按钮
3. 选择"WebShell登录"
4. 等待连接...
5. 自动登录成功！

✅ 无需输入密码，直接进入！
```

---

### 方法2：使用本地终端（传统方式）

**Mac/Linux用户**：
```bash
# 打开终端
# 输入以下命令（替换IP为你的服务器IP）

ssh root@43.XXX.XXX.XXX

# 首次连接会提示：
# Are you sure you want to continue connecting (yes/no)?
# 输入：yes

# 然后输入你设置的密码
```

**Windows用户**：
```powershell
# 打开PowerShell
# 输入以下命令

ssh root@43.XXX.XXX.XXX

# 输入密码
```

---

### 6.3 验证登录成功

登录成功后，你会看到：

```
Welcome to Ubuntu 22.04 LTS

Last login: ...

root@biostats-server:~#  ← 这个提示符说明登录成功！
```

✅ 连接成功！你现在在服务器上了！

---

## ✅ 第7步：准备部署环境（5分钟）

**现在在服务器终端中执行以下命令**：

### 7.1 更新系统

```bash
# 更新软件包列表
apt update

# 等待完成...
```

---

### 7.2 检查Docker（如果选了Docker CE镜像）

```bash
# 检查Docker版本
docker --version

# 应该显示：Docker version 24.x.x

# 检查Docker Compose
docker-compose --version

# 如果没有，安装：
apt install docker-compose -y
```

---

### 7.3 如果选的是Ubuntu镜像，安装Docker

```bash
# 一键安装Docker
curl -fsSL https://get.docker.com | sh

# 安装Docker Compose
apt install docker-compose -y

# 验证安装
docker --version
docker-compose --version
```

---

## ✅ 第8步：部署应用（10分钟）

### 8.1 克隆项目代码

**如果你已经推送到GitHub**：
```bash
# 克隆GitHub仓库（替换YOUR_USERNAME）
cd /opt
git clone https://github.com/YOUR_USERNAME/biostats-terminator.git
cd biostats-terminator
```

**如果还没有推送到GitHub**：
```
我们先创建一个简单版本测试，
稍后再上传完整代码
```

---

### 8.2 创建测试项目（临时方案）

```bash
# 创建项目目录
mkdir -p /opt/biostats-test
cd /opt/biostats-test

# 创建简单的测试应用
cat > app.py << 'EOF'
from flask import Flask

app = Flask(__name__)

@app.route('/')
def index():
    return '''
    <html>
    <head>
        <meta charset="UTF-8">
        <title>Biostats终结者</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                text-align: center;
                padding: 50px;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
            }
            .container {
                background: white;
                color: #333;
                padding: 40px;
                border-radius: 10px;
                max-width: 600px;
                margin: 0 auto;
                box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            }
            h1 { color: #667eea; }
            .status { 
                color: green; 
                font-weight: bold; 
                font-size: 24px; 
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>🔬 Biostats终结者</h1>
            <h2>Professional Biostatistics Analysis Platform</h2>
            <p class="status">✓ 服务器运行正常</p>
            <p>腾讯云部署成功！</p>
            <hr>
            <p>完整功能部署中...</p>
        </div>
    </body>
    </html>
    '''

@app.route('/health')
def health():
    return {'status': 'healthy', 'server': 'Tencent Cloud'}

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=8000)
EOF
```

---

### 8.3 创建Docker配置

```bash
# 创建Dockerfile
cat > Dockerfile << 'EOF'
FROM python:3.11-slim

WORKDIR /app

RUN pip install flask gunicorn

COPY app.py .

EXPOSE 8000

CMD ["gunicorn", "--bind", "0.0.0.0:8000", "app:app"]
EOF
```

---

### 8.4 构建并运行

```bash
# 构建Docker镜像
docker build -t biostats-app .

# 运行容器
docker run -d --name biostats \
  -p 8000:8000 \
  --restart unless-stopped \
  biostats-app

# 查看容器状态
docker ps

# 应该看到biostats容器在运行
```

---

## ✅ 第9步：测试访问（1分钟）

### 9.1 本地测试

**在服务器上测试**：
```bash
curl http://localhost:8000

# 应该返回HTML内容
```

---

### 9.2 外网访问

**在你的电脑浏览器中打开**：
```
http://你的服务器IP:8000

例如：
http://43.138.123.45:8000
```

**你应该看到**：
```
🔬 Biostats终结者
Professional Biostatistics Analysis Platform
✓ 服务器运行正常
腾讯云部署成功！
```

🎉 **恭喜！部署成功了！**

---

## ✅ 第10步：配置Nginx反向代理（可选，5分钟）

### 10.1 安装Nginx

```bash
apt install nginx -y
```

---

### 10.2 配置Nginx

```bash
cat > /etc/nginx/sites-available/biostats << 'EOF'
server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
EOF

# 启用配置
ln -sf /etc/nginx/sites-available/biostats /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default

# 测试配置
nginx -t

# 重启Nginx
systemctl restart nginx
```

---

### 10.3 现在可以直接访问

```
http://你的IP地址

不需要加端口号了！
```

---

## 🎉 部署完成！

### 你现在拥有：

✅ 腾讯云轻量服务器（2核4GB）
✅ Ubuntu 22.04 + Docker环境
✅ 可访问的Web应用
✅ 防火墙已配置
✅ Nginx反向代理

### 访问地址：

```
http://你的服务器IP
```

---

## 📝 部署信息汇总

```
╔═══════════════════════════════════════════╗
║          部署完成信息                      ║
╠═══════════════════════════════════════════╣
║                                           ║
║ 云服务商: 腾讯云                           ║
║ 套餐: 轻量应用服务器 2核4GB               ║
║ 费用: ¥88/年（首年）                       ║
║                                           ║
║ 服务器IP: ___.___.___.___ ← 你的IP        ║
║                                           ║
║ 访问地址: http://你的IP                    ║
║                                           ║
║ SSH登录: ssh root@你的IP                  ║
║ 密码: ___________________                 ║
║                                           ║
║ 部署时间: ____年__月__日                   ║
║ 到期时间: 2027年2月7日                     ║
║                                           ║
╚═══════════════════════════════════════════╝
```

---

## 🎯 下一步

### 今天可以做：
- [ ] 测试访问网站
- [ ] 记录所有密码和配置
- [ ] 截图保存服务器信息

### 明天可以做：
- [ ] 上传完整项目代码到GitHub
- [ ] 部署完整的Biostats平台
- [ ] 配置数据库
- [ ] 添加域名（可选）

---

## 🆘 常见问题

**Q: 访问不了网站？**
```bash
# 检查容器运行
docker ps

# 检查防火墙
# 确保8000端口和80端口已开放

# 查看日志
docker logs biostats
```

**Q: 忘记密码？**
```
腾讯云控制台 → 实例列表 → 更多 → 重置密码
```

**Q: 想换镜像？**
```
腾讯云控制台 → 实例列表 → 更多 → 重装系统
```

---

**恭喜你完成腾讯云部署！只花了¥88就拥有了自己的云服务器！** 🎊

**告诉我你的IP地址，我帮你检查部署是否成功！** 📍
